import React, { FC } from "react";
import Layout from "../../components/Layout";

const OrdersPage: FC<{}> = ({}) => {
  return <Layout heading="Распоряжения">
    
  </Layout>;
};

export default OrdersPage;
